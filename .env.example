# AirflowHome external providers are not required during M0/M0.5.
# Add provider-specific variables only when the corresponding milestone activates them.

# VITE_MAP_PROVIDER_KEY=
# VITE_WEATHER_PROVIDER_KEY=
